services:
  dashcord:
    build: .
    container_name: dashcord
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env
      - ./routes.json:/app/routes.json
      - ./config:/app/config